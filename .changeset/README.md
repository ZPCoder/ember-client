# Changesets

Every behavior or public client-port change must include a Changeset. The
client is not published as a runtime dependency, but its release version is
recorded in the `ember-ops` compatibility matrix.
